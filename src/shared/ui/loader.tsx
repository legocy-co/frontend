const Loader = () => (
  <div className="w-full h-full flex justify-center items-center flex-grow">
    <span className="loading loading-dots loading-lg text-primary-default" />
  </div>
);

export default Loader;
